# Historical chronological-split artifacts

These XGBoost-specific importance artifacts belong to the older chronological split. They are retained for provenance and are not loaded by the current workflow. Use the generic `07_final_*` artifacts in `reports/model_results/` and the current report.
